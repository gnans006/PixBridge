# PixBridge local dev secrets template — THIS FILE IS COMMITTED
# Copy to .env, fill in your values, then run: .\setup.ps1
#
# The actual .env is gitignored — never commit it.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pixbridge_dev
DB_USER=postgres
DB_PASSWORD=YOUR_POSTGRES_PASSWORD_HERE
